{
  "name": "cascadeudfs",
  "version": "1.0.0",
  "description": "Copy values from UDFs in top-level records to the same UDFs in corresponding contact records.",
  "main": "getCascadeUdfs.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "udf",
    "copy"
  ],
  "author": "Patrick Wong",
  "license": "ISC",
  "dependencies": {
    "cascadeudfs": "file:"
  }
}
